{
  "private": true,
  "scripts": {
    "ready": "vp fmt && vp run -r build && vp lint && vp run -r test",
    "build": "vp run -r build",
    "test": "vp run -r test",
    "prepare": "vp config",
    "format": "vp fmt"
  },
  "devDependencies": {
    "prettier": "3.8.1",
    "vite-plus": "0.1.24"
  },
  "packageManager": "pnpm@11.6.0"
}
